# Use Render-provided port in production; fallback to 8080 locally
server.port=${PORT:8080}

# Allow CORS from deployed frontend and local development
spring.web.cors.allowed-origins=https://typing-substring-match-game.onrender,http://127.0.0.1:8081

# Optional: logging level
logging.level.org.springframework=INFO

